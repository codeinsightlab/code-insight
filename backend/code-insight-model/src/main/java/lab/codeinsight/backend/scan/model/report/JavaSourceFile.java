package lab.codeinsight.backend.scan.model.report;

/**
 * Loaded Java source with normalized path and package metadata.
 */
public record JavaSourceFile(String filePath, String packageName, String sourceCode) {
}
